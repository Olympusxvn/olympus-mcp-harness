# Learner Profile

## Who They Are
Contestant in The WebMCP Challenge (deadline 3 Sep 2026, 1:00pm PDT). Founder, self-taught, passionate about AI. Name not shared. Entered as a competitor, not a spectator. Product thesis is already locked in `PROJECT.md` (MCP Harness / Olympus MCP Harness): execution control behind the WebMCP boundary. MemWal / Walrus / blockchain are explicitly out of scope.

## Technical Experience
Experience level: self-taught founder — treat as intermediate-to-experienced on product/architecture, stretching on WebMCP as a live browser API. Stack locked from `PROJECT.md`: Next.js, React, TypeScript, Tailwind CSS, Chrome WebMCP imperative API, Zod/JSON Schema, in-memory MVP state, Vercel deploy. Wants the agent to build against that document rather than re-litigate stack or idea. AI coding agents: currently in Cursor; expects the agent to follow the spec-driven command chain and the existing strategy doc.

## Learning Goals
Walk away with real WebMCP (imperative `document.modelContext.registerTool`, Chrome 149+ / ChatGPT in-app browser) and a working execution harness (discover → validate → authorize → execute → verify + human approval). Not a fake demo wrapper.

## Creative Sensibility
Luxury VIP lounge, not generic dark-tech dashboard. Follow the `luxury-wc-ui` skill: obsidian/midnight base, gold (`#FFD700`) + champagne/platinum accents, frosted glass (`.luxe-glass`), Sora display + Inter body, 22px cards / 16px tiles / pill buttons. No neon. Brand copy stays MCP Harness (`The model reasons. The machine executes.`) — do not import Walrus / World Cup titles. Note: `PROJECT.md` §13 suggested blue/purple/green stage colors; learner overrode this with luxury gold/glass. Resolve in `/scope` without breaking stage readability.

## Prior SDD Experience
First time through a full spec-driven chain. Only artifact before this curriculum: `PROJECT.md` (strategy locked). No PRD, spec, or checklist yet. Calibrate `/reflect` as first-time SDD; do not over-explain basics they already encoded in the strategy doc, but do keep the command chain intact so those documents become submission evidence.
