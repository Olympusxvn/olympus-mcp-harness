# Process Notes

## /onboard

- Technical experience: self-taught founder; stack taken as locked from PROJECT.md (Next.js / TS / Tailwind / WebMCP imperative / Vercel). Did not want to re-discuss stack — “build theo PROJECT.md”.
- Learning goals: real WebMCP + a real execution harness, not a cosmetic demo.
- Creative sensibility: luxury-wc-ui (obsidian, gold, frosted glass). Explicit override of PROJECT.md §13 blue/purple/green palette. Brand copy stays MCP Harness; no Walrus/World Cup titles. MemWal called out as unrelated.
- Prior SDD: none beyond PROJECT.md. First full command chain.
- Context: contestant in The WebMCP Challenge; thesis already locked (model reasons / machine executes). Folder already had PROJECT.md + architecture/roadmap images — continued in place.
- Energy: impatient to build, short answers, high conviction. Keep interviews tight; do not re-open the product thesis.

## /scope

- Learner skipped the interview: “Bạn viết spec, plan.” Distilled `PROJECT.md` + onboard decisions into `docs/scope.md`.
- Deepening rounds: 0. Extra context did not come from new conversation; it came from the locked strategy doc.
- Active shaping: they drove by refusing to re-litigate the idea and by locking luxury-wc-ui + no MemWal.
- Cuts: marketplace/agent-OS/blockchain/extra tools/declarative API. Kept five commerce tools + harness + simulate-agent fallback for judges without WebMCP.
- Palette conflict resolved in scope: gold/champagne chips + labels; deep red for errors; no neon RGB stages.

## /prd

- Expanded PROJECT.md behaviors into epics/stories/acceptance without a live PRD interview (learner delegated).
- Deepening rounds: 0.
- Scope guard: in-memory, simulated checkout, no get_cart unless needed. Wow moment = approval modal.
- Open: ChatGPT return-shape confirmation during build; MIT default.

## /spec

- Stack unchanged: Next.js / TS / Tailwind / Zod / client harness / Vercel / webmcp-types.
- WebMCP: raw `registerTool` + AbortSignal (not use-webmcp-tool unless Strict Mode duplicates tools).
- Simulate panel shares `harness.run` so Execution judging is not WebMCP-client-dependent.
- Deepening rounds: 0. Researched Chrome imperative API (Aug 20, 2026) and webmcp-types 0.1.5.
- Active shaping: architecture deferred to PROJECT.md + learner UI override.

## /checklist

- Sequence follows PROJECT.md phases: shell → types/policy → pipeline → tools → approval → WebMCP → trace → failure → tests/README → Vercel → Devpost.
- Mode: Autonomous + verification checkpoints after items 3, 6, 9, 12. No comprehension checks. Commit-after-item encoded for /build sessions where commits are allowed.
- 12 items; larger than curriculum 15–30m slices because this is a 10-day challenge MVP.
- Deepening rounds: 0. Learner did not gut-check item count; agent chose 12 from the 8-phase roadmap.
- Submission: keep human-chosen name MCP Harness; video required by this challenge even though the generic checklist template calls video optional.

